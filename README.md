Here are both my files for my system interface and for my statistical analysis.
You will need the SAGAT and TrustScores file along with thesisAnalysis to run the analysis.
Since you asked us to turn in user data seperately, that will need to be put inside the same folder als the rest of the files.
The user data should be in a folder called Data. 
The other three files (the CSS, HTML, and Javascript files) are used for running the system interface.
